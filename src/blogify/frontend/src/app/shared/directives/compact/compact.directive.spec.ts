import { CompactDirective } from './compact.directive';

describe('CompactDirective', () => {
  it('should create an instance', () => {
    const directive = new CompactDirective();
    expect(directive).toBeTruthy();
  });
});
